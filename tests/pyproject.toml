[tool.poetry]
name = "tests"
version = "0.1.0"
description = ""
authors = ["Hardik <hardik.jivani@nyu.edu>"]

[tool.poetry.dependencies]
python = "^3.8"
pytest = "^7.4.3"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
